# tasks.md — ReportBot: баги и улучшения

Структурировано по модулям проекта (см. memory-bank.md, раздел 3 "Структура файлов").

## 1. Учащиеся (`app/bot/handlers/admin/students.py`)


- [x] Сортировка списка учащихся по номерам/цифрам (сейчас, вероятно, сортировка по алфавиту/дате добавления) потому что при удалении ученика и при создании нового айди не сортируется, то есть если ты удалил учащегося с айди один и добавил нового то новый будет айди 2
- [x] Добавление учащихся списком (пакетный ввод нескольких ФИО за раз, а не по одному через AddStudentStates) список формата фио/nфио/n

## 2. Педагоги и админы (`app/bot/handlers/admin/roles.py`)

- [x] Полное удаление педагога из БД вместо soft-delete (сейчас — `is_active=False` через `user_service.deactivate`)
- [x] Сортировка списка педагогов (по департаменту / алфавиту — уточнить критерий)
- [x] Отдельный список админов, помимо списка педагогов (сейчас в UI виден только список педагогов)
- [x] Проблема добавления пользователя через поиск: поиск Telegram глючит на устройстве куратора (ограничение клиента TG, не баг бота); `@username` не срабатывает, т.к. бот не может получить numeric Telegram ID по username без предварительного контакта — нужно продумать альтернативный флоу (например, добавление по forward-сообщению или по /start-регистрации самого пользователя с последующим подтверждением админом)

## 3. Контекст департамента (`app/bot/handlers/teacher/shift.py`, состояния `ShiftSelectStates`)

- [ ] При нажатии "изменить контекст" (`teacher:context:change`) — сначала показывать текущий (старый) контекст, а не сразу пустое поле ввода
- [ ] При дополнении контекста голосом — отображать текущий контекст рядом/перед новым вводом (сейчас не видно, что уже есть)
- [ ] Возможность полностью удалить контекст департамента (сейчас есть только редактирование/дополнение через `revise_shift_context`)
- [ ] В промт LLM (`beautify_shift_context` / `generate_report`) передавать строго название департамента (`get_department_name`), 



## 5. Анкета и отчёты (`app/bot/handlers/teacher/questions.py`, `generation.py`)

- [ ] При выборе детей показывать прогресс — сколько вопросов заполнено из 13 (счётчик по таблице `answers` для данного `student_id`)
- [ ] Ручное редактирование текста отчёта (сейчас правки только через диалог с LLM `revise_report` — неудобно и затратно по токенам для точечных правок одного слова)
- [ ] Возможность редактирования уже финализированного отчёта (`report:finalize` сейчас, судя по всему, блокирует дальнейшие правки — `is_finalized=True`)
- [ ] при редактировании отчёта должен показыватся полный текст отчёта